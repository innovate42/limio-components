import React from 'react';
import InlineAlert from './inline-alert';

export default {
  title: 'common/Inline Alert',
  component: InlineAlert,
  args: {
    text: 'Inline Alert',
  },
};

// eslint-disable-next-line react/prop-types
export const inlineAlert = args => <InlineAlert {...args} />;
