import React from 'react';
import ControlCheck from './control-check';

export default {
  title: 'common/Control Check',
  component: ControlCheck,
  args: {
    label: 'This is an Control Check',
    id: 'checkboxId',
  },
};

// eslint-disable-next-line react/prop-types
export const controlCheck = args => <ControlCheck {...args} />;
