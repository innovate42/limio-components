import React from 'react';
import Tag from './tag';

export default {
  title: 'common/Tag',
  component: Tag,
  args: {
    text: 'New',
  },
};

// eslint-disable-next-line react/prop-types
export const tag = args => <Tag {...args} />;
