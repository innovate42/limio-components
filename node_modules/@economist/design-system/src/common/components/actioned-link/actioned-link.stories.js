import React from 'react';
import ActionedLink from './actioned-link';

export default {
  title: 'common/Actioned Link',
  component: ActionedLink,
  args: {
    text: 'This is an Actioned Link',
    icon: 'indicator',
    href: '#',
  },
};

// eslint-disable-next-line react/prop-types
export const actionedLink = args => <ActionedLink {...args} />;
