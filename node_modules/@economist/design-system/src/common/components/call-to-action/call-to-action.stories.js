import React from 'react';
import CallToAction from './call-to-action';

export default {
  title: 'common/Call To Action',
  component: CallToAction,
  args: {
    text: 'This is a CTA',
    onClick: {},
  },
};

// eslint-disable-next-line react/prop-types
export const callToAction = args => (
  <div
    style={
      args.inverse
        ? { backgroundColor: 'darkgrey', padding: '1rem' }
        : { backgroundColor: 'white', padding: '1rem' }
    }
  >
    <CallToAction {...args} />
  </div>
);
