---
name: Ident (Columns)
route: /components/common/idents-columns
menu: Idents
edit: false
---

import { Playground, Props } from 'docz';
import IdentColumns from './ident-columns';
import { IDENT_NAMES } from '../assets/idents-columns';

# Ident
**SUBCATEGORY:** Identity

<hr />

## Ident (Columns)

**Single usage**
<Playground>
  <IdentColumns ident='erasmus' />
</Playground>

**List of all Idents**
<Playground>
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(120px, 1fr))', gridGap: '2rem 1rem' }}>
    {IDENT_NAMES.map(identName => (
      <div key={identName} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', textAlign: 'center' }}>
        <IdentColumns ident={identName} />
        <code style={{ marginTop: '1rem', fontFamily: 'monospace', fontSize: '1.2rem' }}>{identName}</code>
      </div>
    ))}
  </div>
</Playground>

<hr />

## Properties

<Props of={IdentColumns} />
