---
name: Ident (Newsletters)
route: /components/common/idents-newsletters
menu: Idents
edit: false
---

import { Playground, Props } from 'docz';
import IdentNewsletters from './ident-newsletters';
import { IDENT_NAMES } from '../assets/idents-newsletters';

# Ident
**SUBCATEGORY:** Identity

<hr />

## Ident (Newsletters)

**Single usage**
<Playground>
  <IdentNewsletters ident='the-economist-this-week' />
</Playground>

**List of all Idents**
<Playground>
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(120px, 1fr))', gridGap: '2rem 1rem' }}>
    {IDENT_NAMES.map(identName => (
      <div key={identName} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', textAlign: 'center' }}>
        <IdentNewsletters ident={identName} />
        <code style={{ marginTop: '1rem', fontFamily: 'monospace', fontSize: '1.2rem' }}>{identName}</code>
      </div>
    ))}
  </div>
</Playground>

<hr />

## Properties

<Props of={IdentNewsletters} />
