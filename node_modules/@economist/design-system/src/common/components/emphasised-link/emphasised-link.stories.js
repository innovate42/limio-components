import React from 'react';
import EmphasisedLink from './emphasised-link';

export default {
  title: 'common/Emphasised Link',
  component: EmphasisedLink,
  args: {
    text: 'This is an Emphasised Link',
    onClick: {},
  },
};

// eslint-disable-next-line react/prop-types
export const emphasisedLink = args => (
  <div
    style={
      args.inverse
        ? { backgroundColor: 'darkgrey', padding: '1rem' }
        : { backgroundColor: 'white', padding: '1rem' }
    }
  >
    <EmphasisedLink {...args} />
  </div>
);
