import React from 'react';
import StatusAlert from './status-alert';

export default {
  title: 'common/Status Alert',
  component: StatusAlert,
  args: {
    text: 'Status Alert',
  },
};

export const statusAlert = args => <StatusAlert {...args} />;
