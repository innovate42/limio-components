import React from 'react';
import SectionControl from './section-control';

export default {
  title: 'common/Section Control',
  component: SectionControl,
  args: {
    children: ['Hello this is some children'],
    sectionTitle: 'This is a section title',
    inverse: false,
  },
};

export const sectionControl = args => <SectionControl {...args} />;
