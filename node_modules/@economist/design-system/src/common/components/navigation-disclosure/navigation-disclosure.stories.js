import React from 'react';
import NavigationDisclosure from './navigation-disclosure';

export default {
  title: 'common/Navigation Disclosure',
  component: NavigationDisclosure,
  args: {
    text: 'Navigation Disclosure',
    onClick: {},
  },
};

// eslint-disable-next-line react/prop-types
export const navigationDisclosure = args => (
  <div
    style={
      args.inverse
        ? { backgroundColor: 'darkgrey', padding: '1rem' }
        : { backgroundColor: 'white', padding: '1rem' }
    }
  >
    <NavigationDisclosure {...args} />
  </div>
);
