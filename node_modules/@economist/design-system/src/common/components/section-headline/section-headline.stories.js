import React from 'react';
import SectionHeadline from './section-headline';

export default {
  title: 'common/Section Headline',
  component: SectionHeadline,
  args: {
    headline: 'This is a headline',
    subheadline: 'This is a subheadline',
  },
};

export const sectionHeadline = args => (
  <div
    style={
      args.inverse
        ? { backgroundColor: 'darkgrey', padding: '1rem' }
        : { backgroundColor: 'white', padding: '1rem' }
    }
  >
    <SectionHeadline {...args} />
  </div>
);
