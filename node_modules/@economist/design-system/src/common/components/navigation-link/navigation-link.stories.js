import React from 'react';
import NavigationLink from './navigation-link';

export default {
  title: 'common/Navigation Link',
  component: NavigationLink,
  args: {
    text: 'This is a Navigation Link',
    onClick: {},
  },
};

// eslint-disable-next-line react/prop-types
export const navigationLink = args => (
  <div
    style={
      args.inverse
        ? { backgroundColor: 'darkgrey', padding: '1rem' }
        : { backgroundColor: 'white', padding: '1rem' }
    }
  >
    <NavigationLink {...args} />
  </div>
);
