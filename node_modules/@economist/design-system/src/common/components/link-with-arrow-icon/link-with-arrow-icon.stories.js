import React from 'react';
import LinkWithArrowIcon from './link-with-arrow-icon';

export default {
  title: 'common/Link With Arrow Icon',
  component: LinkWithArrowIcon,
  args: {
    text: 'This is a Link with Arrow Icon',
    onClick: {},
  },
};

// eslint-disable-next-line react/prop-types
export const linkWithArrowIcon = args => (
  <div
    style={
      args.inverse
        ? { backgroundColor: 'darkgrey', padding: '1rem' }
        : { backgroundColor: 'white', padding: '1rem' }
    }
  >
    <LinkWithArrowIcon {...args} />
  </div>
);
