import React from 'react';
import LinkWithArrow from './link-with-arrow';

export default {
  title: 'common/Link With Arrow',
  component: LinkWithArrow,
  args: {
    text: 'This is a Link with Arrow',
    onClick: {},
  },
};

// eslint-disable-next-line react/prop-types
export const linkWithArrow = args => (
  <div
    style={
      args.inverse
        ? { backgroundColor: 'darkgrey', padding: '1rem' }
        : { backgroundColor: 'white', padding: '1rem' }
    }
  >
    <LinkWithArrow {...args} />
  </div>
);
