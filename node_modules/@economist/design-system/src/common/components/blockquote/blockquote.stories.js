import React from 'react';
import Blockquote from './blockquote';

export default {
  title: 'common/Blockquote',
  component: Blockquote,
  args: {
    text: 'This is an example of a Blockquote.',
  },
};

// eslint-disable-next-line react/prop-types
export const blockquote = args => <Blockquote {...args} />;
